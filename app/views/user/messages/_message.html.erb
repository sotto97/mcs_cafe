<div class="messages list-group-item <%= "right" if message.user.id == current_user.id %>">
	<%= attachment_image_tag message.user, :profile_image, :size =>'40x40', format: 'jpeg''jpg''svg''png', fallback: "avator.svg", class: "img-circle" %>
  <p><%= "#{message.body}" %></p>
</div>

<style>
	.messages p{
		background-color: #eee;
	}
	.right{
		text-align: right;
	}
</style>